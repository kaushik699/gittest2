pipeline{
    agent {
        label 'Dev'
    }
    stages{
        stage('Build'){
            steps{
                sh 'echo "Hello Jenkins, this is running the agents and check"'
            }
        }
    }
}